class Solution {
    public boolean solution(int x) {
        int sum = 0;
        String [] xString = String.format("%s", x).split("");
        
        for(int i = 0; i < xString.length; i++) {
            sum += Integer.parseInt(xString[i]);
        }
 
        return x % sum == 0;
    }
}
